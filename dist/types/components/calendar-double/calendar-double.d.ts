import { EventEmitter } from '../../stencil-public-runtime';
import { CalendarEntry } from './../../utils/interfaces/calendarEntry';
import { PositionRange } from '../../utils/enum/positionRange';
export declare class CalendarDouble {
  private firstDayForRange;
  private lastDayForRange;
  private daySelectedInMain;
  private daySelectedInSecondary;
  private datesInMain;
  private datesInSecondary;
  typeSelectionMain: 'oneDay' | 'range' | 'period';
  typeSelectionSecondary: 'oneDay' | 'range' | 'period';
  setCalendarMain: CalendarEntry;
  setCalendarSecond: CalendarEntry;
  calendarActive: boolean;
  rangeMain: number[];
  positionRangeMain: PositionRange[];
  positionRangeSecondary: PositionRange[];
  mainDateReceived: Date;
  handlerChangeDateReceived(): void;
  typeSelection: 'oneDay' | 'range' | 'period';
  handlerTypeSelection(newType: string, oldType: string): void;
  previousMonthCalendarEvent(): void;
  nextMonthCalendarEvent(): void;
  calendarSingleDaySelected(event: CustomEvent): void;
  calendarDoubleSetDate: EventEmitter<CalendarEntry>;
  applicationDate: EventEmitter<CalendarEntry>;
  rangeDate: EventEmitter<CalendarEntry[]>;
  private assignValuePositionOneDay;
  private setForOneDay;
  private addDateOnSelectedCalendar;
  private cleanDatesInMain;
  private cleanDatesInSecondary;
  private assignValueFromSelectedRange;
  private setRangeOfDaysInASingleCalendar;
  private setRangeDayOnBothCalendars;
  private sortRangeSelection;
  private cleanPreviousSelection;
  private getDateNow;
  private setDateCalendarSecond;
  private setDate;
  componentWillLoad(): void;
  render(): any;
}
