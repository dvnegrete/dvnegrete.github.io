import { EventEmitter } from '../../stencil-public-runtime';
import { CalendarEntry } from '../../utils/interfaces/calendarEntry';
import { PositionRange } from '../../utils/enum/positionRange';
export declare class CalendarSingle {
  private dayNames;
  private monthNames;
  private totalDaysInTheMonth;
  private year;
  setCalendar: CalendarEntry;
  private baseNameMonth;
  dateCalendar: CalendarEntry;
  numberCalendar: 'main' | 'secondary';
  positionRange: PositionRange[];
  typeSelection: 'oneDay' | 'range' | 'period';
  handlerTypeSelection(newType: string, oldType: string): void;
  valueCalendar: CalendarEntry;
  daysInMonth: number[];
  sendFromThisCalendar: CalendarEntry;
  calendarActive: boolean;
  handlerWatchProp(): void;
  valueCalendarSelected: EventEmitter<any>;
  setCalendarChange(newValue: CalendarEntry, oldValue: CalendarEntry): void;
  handlerChangeCleanPeriod(): void;
  handlerCleanCalendarSelection(): void;
  private startDay;
  private monthOfThirtyOneDays;
  private monthOfThirtyDays;
  private isLeap;
  private getTotalDays;
  private writeMonth;
  componentWillLoad(): void;
  private dayCalendarIsNow;
  private verifyLimit;
  private daySelectedHandler;
  validateOneDay(): boolean;
  validateRange(): boolean;
  validateTwoPositionsOfRange(day: number): boolean;
  validatePeriod(): boolean;
  private nameClassToElement;
  private daysInMonthRender;
  render(): any;
}
